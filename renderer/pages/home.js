import BibleNavigation from '@/modules/biblenavigation/BibleNavigation';
import Meta from '../src/Meta';

const home = () => (
  <div>
    <Meta />
    <BibleNavigation />
  </div>
);

export default home;
