import React from 'react';

const ReferenceSelector = props => {
    console.log("selector", props)
    return (
        <div>
            {/* pass value properly with the help props to change the reference content */}
        </div>
    );
};

export default ReferenceSelector;